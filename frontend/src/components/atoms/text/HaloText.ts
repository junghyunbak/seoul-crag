import { styled, Typography } from '@mui/material';

export const Halo = styled(Typography)({
  textShadow: '-1px 0 white, 0 1px white, 1px 0 white, 0 -1px white',
  lineHeight: 1,
  textAlign: 'center',
  fontWeight: 'bold',
});
